// Command line arguments are optional string arguments that are passed by the
// operating system to the program when it is launched. The program can then use
// them as input (or ignore them). Much like function parameters provide a way
// for a function to provide inputs to another function, command line arguments
// provide a way for people or programs to provide inputs to a program.

// The new form of main():

// argc = argument count
// argv = argument vectors, array of c-style strings
// length of argv is argc
int main(int argc, char* argv[])
{
    // we can use the passed command line parameters here
    return 0;
}